using System.Collections;
using System.Collections.Generic;
using UnityEditor;
using UnityEngine;

[CustomEditor(typeof(HeroPower))]
public class HeroPowerEditor : Editor {
    // Class Only exists to allow CompareEntity classes to be drawn properly on hero power
}
